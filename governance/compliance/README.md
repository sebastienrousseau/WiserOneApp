# Compliance Records

This directory stores software validation procedure and record assets.

Files:

- `software-validation-procedure.md`: Validation workflow for release decisions.
- `validation-record-template.json`: Schema-compatible template for release validation records.
- Runtime-generated validation evidence: `governance/sbom/validation-record.json`.

Validation records are generated by:

```sh
make generate-validation-record
```
